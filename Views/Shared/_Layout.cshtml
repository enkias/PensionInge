<!DOCTYPE html>
<html lang="cs">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@(ViewData["Title"] ?? "Pension Inge – Vyšší Brod · Šumava · Vltava")</title>
    <meta name="description" content="@(ViewData["Description"] ?? "Pension a Restaurace Inge ve Vyšším Brodě na břehu Vltavy. Ubytování, restaurace, půjčovna lodí. Rezervujte přímo a ušetřete.")" />
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        river: { 50:'#f0fdfa',100:'#ccfbf1',500:'#14b8a6',600:'#0d9488',700:'#0f766e',800:'#115e59',900:'#134e4a' },
                        stone:  { 50:'#fafaf9',100:'#f5f5f4',200:'#e7e5e4',300:'#d6d3d1',400:'#a8a29e',500:'#78716c',600:'#57534e',700:'#44403c',800:'#292524',900:'#1c1917' },
                    },
                    fontFamily: { display: ['Georgia','serif'] }
                }
            }
        }
    </script>
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.14.8/dist/cdn.min.js"></script>
    <style>
        [x-cloak] { display: none !important; }
        html { scroll-behavior: smooth; }
        .lang-bar a { opacity: .65; transition: opacity .15s; }
        .lang-bar a:hover, .lang-bar a.active { opacity: 1; }
    </style>
</head>
<body class="bg-stone-50 text-stone-800 font-sans">

    <!-- Sticky navbar + jazyková lišta -->
    <div x-data="{ scrolled: false }" x-init="window.addEventListener('scroll', () => scrolled = window.scrollY > 80)">

        <!-- Sticky navbar (po scrollu) -->
        <div x-show="scrolled"
             x-transition:enter="transition duration-200"
             x-transition:enter-start="opacity-0 -translate-y-2"
             x-transition:enter-end="opacity-100 translate-y-0"
             class="fixed top-0 left-0 right-0 z-50 bg-river-800/95 backdrop-blur-sm shadow-lg">
            <div class="max-w-7xl mx-auto px-6 py-3 flex items-center justify-between">
                <a href="/" class="text-white font-display font-bold text-lg">Pension Inge</a>
                <div class="hidden md:flex items-center gap-5 text-sm text-white/80">
                    <a href="/restaurace" class="hover:text-white transition-colors">Restaurace</a>
                    <a href="/vylety" class="hover:text-white transition-colors">Výlety</a>
                    <a href="/galerie" class="hover:text-white transition-colors">Galerie</a>
                    <a href="#rezervace" class="bg-white/20 hover:bg-white/30 text-white px-4 py-1.5 rounded-full transition-colors font-semibold">Rezervovat přímo</a>
                    <div class="flex items-center gap-2 pl-2 border-l border-white/20">
                        <a href="#" title="Česky"><img src="https://flagcdn.com/w20/cz.png" width="20" height="14" alt="CZ" class="rounded-sm opacity-100" /></a>
                        <a href="#" title="English"><img src="https://flagcdn.com/w20/gb.png" width="20" height="14" alt="EN" class="rounded-sm opacity-60 hover:opacity-100" /></a>
                        <a href="#" title="Deutsch"><img src="https://flagcdn.com/w20/de.png" width="20" height="14" alt="DE" class="rounded-sm opacity-60 hover:opacity-100" /></a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Plovoucí jazyková lišta (jen v hero, bez scrollu) -->
        <div x-show="!scrolled" class="lang-bar fixed top-3 right-4 z-[60] flex items-center gap-2 bg-black/30 backdrop-blur-sm rounded-full px-3 py-1.5">
            <a href="#" title="Česky"   class="active"><img src="https://flagcdn.com/w20/cz.png" width="20" height="14" alt="CZ" class="rounded-sm" /></a>
            <a href="#" title="English"><img src="https://flagcdn.com/w20/gb.png" width="20" height="14" alt="EN" class="rounded-sm" /></a>
            <a href="#" title="Deutsch"><img src="https://flagcdn.com/w20/de.png" width="20" height="14" alt="DE" class="rounded-sm" /></a>
        </div>

    </div>

    @RenderBody()

    <!-- AI Chatbot widget -->
    <div x-data="ingeChat()" x-cloak class="fixed bottom-6 right-6 z-50 flex flex-col items-end gap-3">
        <div x-show="open"
             x-transition:enter="transition ease-out duration-200"
             x-transition:enter-start="opacity-0 translate-y-4"
             x-transition:enter-end="opacity-100 translate-y-0"
             class="w-80 sm:w-96 bg-white rounded-2xl shadow-2xl border border-stone-200 flex flex-col overflow-hidden"
             style="height:420px">
            <div class="bg-river-800 text-white px-5 py-4 flex items-center justify-between">
                <div>
                    <p class="font-semibold text-sm">Pension Inge – Asistent</p>
                    <p class="text-xs text-river-200">Zeptejte se na cokoli</p>
                </div>
                <button x-on:click="open=false" class="text-river-200 hover:text-white">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
                </button>
            </div>
            <div class="flex-1 overflow-y-auto p-4 space-y-3" x-ref="msgbox">
                <template x-for="m in messages" :key="m.id">
                    <div :class="m.role==='user' ? 'flex justify-end' : 'flex justify-start'">
                        <div :class="m.role==='user'
                            ? 'bg-river-700 text-white rounded-2xl rounded-tr-sm px-4 py-2.5 text-sm max-w-[80%]'
                            : 'bg-stone-100 text-stone-800 rounded-2xl rounded-tl-sm px-4 py-2.5 text-sm max-w-[85%]'"
                             x-text="m.text"></div>
                    </div>
                </template>
                <div x-show="loading" class="flex justify-start">
                    <div class="bg-stone-100 rounded-2xl rounded-tl-sm px-4 py-3">
                        <div class="flex gap-1">
                            <span class="w-2 h-2 bg-stone-400 rounded-full animate-bounce" style="animation-delay:0ms"></span>
                            <span class="w-2 h-2 bg-stone-400 rounded-full animate-bounce" style="animation-delay:150ms"></span>
                            <span class="w-2 h-2 bg-stone-400 rounded-full animate-bounce" style="animation-delay:300ms"></span>
                        </div>
                    </div>
                </div>
            </div>
            <div class="border-t border-stone-100 p-3 flex gap-2">
                <input x-model="input" x-on:keydown.enter="send()"
                       type="text" placeholder="Napište dotaz…"
                       class="flex-1 text-sm border border-stone-200 rounded-xl px-3 py-2 focus:outline-none focus:border-river-500" />
                <button x-on:click="send()" :disabled="loading || !input.trim()"
                        class="bg-river-700 hover:bg-river-600 disabled:opacity-40 text-white px-4 py-2 rounded-xl text-sm font-semibold transition-colors">
                    Odeslat
                </button>
            </div>
        </div>

        <button x-on:click="open=!open"
                class="w-14 h-14 bg-river-700 hover:bg-river-600 text-white rounded-full shadow-xl flex items-center justify-center transition-all hover:scale-105">
            <svg x-show="!open" class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-3 3v-3z"/></svg>
            <svg x-show="open" class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
        </button>
    </div>

    @await RenderSectionAsync("Scripts", required: false)
    <script>
    function ingeChat() {
        return {
            open: false,
            input: '',
            loading: false,
            messages: [{ id: 0, role: 'assistant', text: 'Dobrý den! Jsem asistent Pensionu Inge. Mohu vám pomoci s informacemi o ubytování, restauraci nebo aktivitách ve Vyšším Brodě.' }],
            async send() {
                const msg = this.input.trim();
                if (!msg || this.loading) return;
                this.messages.push({ id: Date.now(), role: 'user', text: msg });
                this.input = '';
                this.loading = true;
                this.$nextTick(() => { this.$refs.msgbox.scrollTop = 9999; });
                try {
                    const r = await fetch('/api/chat', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ message: msg }) });
                    const d = await r.json();
                    this.messages.push({ id: Date.now() + 1, role: 'assistant', text: d.reply });
                } catch { this.messages.push({ id: Date.now() + 1, role: 'assistant', text: 'Omlouváme se, zkuste to prosím znovu.' }); }
                this.loading = false;
                this.$nextTick(() => { this.$refs.msgbox.scrollTop = 9999; });
            }
        };
    }
    </script>
</body>
</html>
